<!--
CLAUDE.md テンプレート。目標 ≤50 行。
原則: LLM が誤りやすい制約・必須コマンド・依存ファイル参照だけを書く。
アーキ概要の一般論や LLM が既に知っているベストプラクティスは書かない。
剪定テスト: 「この行を消すと Claude がミスするか？」No なら消す。
-->

@<依存ファイル 例: package.json / composer.json / pyproject.toml>

## Language
<応答・コメントの言語方針があれば>

## Architecture
<1〜2 行。詳細はコードに任せる。パスエイリアス等の "誤りやすい" 事実だけ>

## Constraints（LLM が誤りやすい点 — 否定形 + 代替案で書く）
- Do NOT use <Y>. 代わりに <X> を使う
- Do NOT use <禁止 API/パターン>
- <型安全・命名・境界などプロジェクト固有の必須制約>

## Commands
| 用途 | コマンド |
|------|---------|
| test | `<...>` |
| lint | `<...>` |
| typecheck | `<...>` |
| build | `<...>` |
| dev | `<...>` |

## Workflow
開発フロー・レビュー使い分けは `.claude/rules/review-workflow.md` 参照（always-on）。
実装はメインスレッドで TDD。調査は内蔵 Explore、計画は plan mode。
<計画レビューに Codex を使うなら: 計画レビューのみ `@codex-plan-reviewer` に委譲>

## Environment
`.env.example` 参照。主要: <主要な環境変数>
