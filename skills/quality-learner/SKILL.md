---
name: quality-learner
description: "Learn from recurring code review findings and fold them back into the project's planner/coder/reviewer agent files so the same defect is not caught twice. Use after a review session, after a PR cycle with multiple review rounds, or when the user says things like 'we keep getting the same feedback', 'update the agents based on recent reviews', 'learn from this review', or 'add this to the reviewer checklist'. Reads review history (git log, PR comments, recent chat), clusters recurring defect classes, and proposes concrete edits to .claude/agents/reviewer.md, .claude/agents/coder.md, and .claude/agents/planner.md. Never edits agent files without user confirmation."
---

# Quality Learner

## Purpose

Turn review findings into rules. Every time the same class of defect is
caught twice or more, it should stop being a surprise and start being a
pre-flight check. This skill analyzes recent review activity, identifies
recurring patterns, and proposes edits to the project's agent files so
future @coder runs avoid the defect and future @reviewer runs catch it
earlier.

This is the "learning loop" complement to the planner/coder/reviewer set
generated by `claude-code-config-designer`.

## Preconditions

This skill expects the project to already have:

- `.claude/agents/reviewer.md` (required — edit target)
- `.claude/agents/coder.md` (required — edit target)
- `.claude/agents/planner.md` (optional — secondary edit target)

If none of these exist, stop and tell the user to run
`claude-code-config-designer` first (Tier 3) to generate the agent set.

## Workflow

Four sequential steps. Do not skip any.

### Step 1: Gather Review Evidence

Collect the raw material to analyze. Prefer multiple sources:

- **Explicit user input** — if the user pastes review findings or names
  specific issues, use those directly
- **Recent git log on the current branch** — `git log --oneline -30`
  then `git show` on commits with messages containing "fix", "修正",
  "address review", "review feedback", etc.
- **Open PR comments** — `gh pr view <N> --comments` and
  `gh api repos/OWNER/REPO/pulls/N/comments` for inline review comments
- **Recent chat context** — if the current session contains
  @reviewer reports, use them

Normalize each finding into a short record:

```
- File: <path:line>
- Defect class: <one of: type safety | race condition | a11y | input
  validation | SRP | DRY | error handling | test quality | perf | other>
- Specific issue: <short description>
- Source: <git commit / PR comment / user / session>
```

If no evidence can be gathered, ask the user to paste recent review
findings before continuing. Do not proceed with an empty dataset.

### Step 2: Cluster and Rank

Group findings by defect class. Apply these thresholds:

- **Report to the user** any class with **2 or more occurrences**
- **Do not report** one-off findings — they are not yet patterns
- **Always report** any class that caused a Critical or High severity
  report, even as a single occurrence, because the cost is too high to
  wait for recurrence

For each cluster, record:

- Defect class name
- Count and sources
- One representative example with file path
- Whether the existing `reviewer.md` already has a matching checklist
  item (read the file to check)

### Step 3: Propose Edits

For each cluster that clears the threshold, draft concrete edits to:

1. **`.claude/agents/reviewer.md`** — add a bullet to the relevant
   checklist section (type safety, a11y, async/race, external boundary,
   etc.). Include a fragment that describes the check in imperative form.
   If no matching section exists, add the bullet to "Code quality".

2. **`.claude/agents/coder.md`** — add a row to the self-check table in
   "Done check". Keep the row short: `| <area> | <check> |`. This moves
   the check from "caught in review" to "caught at implementation time".

3. **`.claude/agents/planner.md`** — if the defect is the kind that
   benefits from being flagged at design time (e.g., async fetch, UI
   with async operations, external API integration), add a bullet to
   the "Risk Flags" section so the planner annotates it before
   implementation.

**Do not edit the files yet.** Present the diffs as proposals.

Format each proposal like this:

```
### Cluster: <defect class> (count: N)

Evidence:
- <path:line> — <short>
- <path:line> — <short>

Proposed edit to reviewer.md:
  In section "<section>":
  + <new bullet>

Proposed edit to coder.md:
  In the done-check table:
  + | <area> | <check> |

Proposed edit to planner.md (optional):
  In "Risk Flags":
  + - [ ] <flag> → <mitigation>
```

### Step 4: Confirm and Apply

Show the user all proposed edits in one batch and ask for confirmation.
Options to offer:

- **Apply all** — edit all three files with the proposed diffs
- **Apply selectively** — user picks which clusters to fold in
- **Skip** — do not edit; user will handle manually

On "apply", use the Edit tool and report each file that changed. On
"skip", record the decision silently and exit.

Never apply edits without an explicit user go-ahead. These files shape
all future agent behavior, so surprise edits are expensive to undo.

## Output Format

After Step 4, report:

```
## Quality learning summary

Evidence gathered: N findings from <sources>
Clusters identified: M (threshold: 2+ occurrences or 1+ Critical/High)

### Applied
- <defect class>: reviewer.md, coder.md (+ planner.md)

### Skipped
- <defect class>: <reason>

Next steps: run the next review cycle and verify the new checks are
catching issues at the right layer (planner risk flag → coder self-check
→ reviewer checklist).
```

## Principles

- **Two strikes, one rule** — a single mistake is noise; two is a
  pattern worth encoding
- **Earlier is cheaper** — whenever possible, promote a check from
  reviewer → coder → planner so the defect is prevented, not just caught
- **Keep it concrete** — bullets should name the specific symptom, not
  restate general principles. "Ensure `useEffect` + fetch uses
  `AbortController` + `ignore`" beats "Handle async correctly"
- **Confirm before editing** — agent files are load-bearing; never
  modify them silently
- **Stop the bleeding, then learn** — do not run this skill mid-review;
  run it after the review cycle has concluded and the fixes are merged

## When NOT to Use This Skill

- The project has no agent files (redirect to `claude-code-config-designer`)
- The current session is in the middle of implementing a feature —
  wait until the review cycle is complete
- The user only has a single review finding with no recurrence —
  the pattern is not yet established
- The defect is in a one-off script or throwaway code, not in the
  reusable project conventions
