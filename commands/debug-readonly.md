---
description: 原因不明の不具合を読み取り専用で調査（修正はしない）
---

原因不明の不具合を、読み取り専用で調査してください。

- `@debugger` を起動し、症状・再現条件・関連コード経路を調べさせる。
- **修正・コミット・push はしない。** 根本原因・仮説（確度順）・修正方針だけを返す。
- 必要なら読み取り専用のコマンド（テスト再実行・ログ確認）で仮説を検証する。
- 実際の修正はメインスレッドが、診断結果を受けて行う。

$ARGUMENTS
